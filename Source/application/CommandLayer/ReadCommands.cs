namespace DEVICE_CORE.CommandLayer
{
    public enum ReadCommands
    {
        DEVICE_ABORT,
        DEVICE_RESET,
        DEVICE_INFO,
        DEVICE_REBOOT,
        LOAD_HMAC,
        TEST_HMAC,
        SECURITY_CONFIGURATION
    }
}
